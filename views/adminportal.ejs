<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GracefulLiving Dashboard</title>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- css resources page -->
    <link rel="stylesheet" href="/css/adminportal.css">

    <!-- js for resources page -->
    <script src="/js/adminportal.js" defer></script> 

    <style>
        body {
            margin: 0;
            font-family: 'Open Sans', sans-serif; /*using this font general text*/
            min-height: 100vh;
            background: url('/images/admin_background.jpg') center/cover no-repeat fixed;
        }
        .hero,
        .overview h2,
        .overview-box h3 {
            font-family: 'Playfair Display', serif; /*using this font for the headers*/
        }
        main {
            padding-top: 5rem;
            padding-bottom: 5rem;
        }
        .hero {
            color: white;
            text-align: left;
            padding: 50px 50px 20px;
            font-size: 2rem; /*Welcome*/
        }
        .secondary-nav {
            background-color: #dff3f4cc;
            text-align: center;
            padding: 15px 0;
            font-size: 1.2em;
            margin-bottom: 40px; /* Add space below nav */
        }
        .secondary-nav a {
            margin: 0 50px;
            cursor: pointer;
            text-decoration: none;
            color: inherit;
            font-weight: bold;
            transition: color 0.3s;
        }
        .secondary-nav a:hover {
            color: #1d8888;
        }
        .overview {
            background: rgba(230, 243, 248, 0.8);
            margin: 0 auto 120px;
            padding: 40px;
            width: 80%;
            text-align: center;
            border-radius: 0px;
        }
        .overview-container {
            display: flex;
            justify-content: space-around;
            margin-top: 50px;
            margin-bottom: 50px;
        }
        .overview-box {
            background: white;
            padding: 1px;
            width: 30%;
            border-radius: 0px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.211);
        }
        .overview-box h3 {
            margin-bottom: 10px;
            font-weight: 500;
        }
        .content-box {
            padding: 15px;
            height: 150px;
            border-radius: 5px;
            overflow-y: auto;
        }
        .overview h2 {
            font-weight: 500;
        }
    </style>
</head>
<body>
    
    <div class="hero">
        <b>Welcome <%= userName %>!</b>
    </div>
    <div class="secondary-nav">
        <a href="/adminportal">Home</a>
        <a href="/content-management">Content Management</a>
        <a href="/adminportal/session">Session Management</a>
        <a href="/clientmanagement">Client Management</a>
    </div>
    <div class="overview">
        <h2>Quick Overview</h2>
        <div class="overview-container">
            <div class="overview-box">
                <h3>Upcoming Sessions</h3>
                <div class="content-box"><%= upcomingSessions %></div>
            </div>
            <div class="overview-box">
                <h3>Notifications</h3>
                <div class="content-box"><%= notifications %></div>
            </div>
            <div class="overview-box">
                <h3>Recent Activities</h3>
                <div class="content-box"><%= recentActivities %></div>
            </div>
        </div>
    </div> 

    <div class="overview">
  <h2>User List</h2>

  <input id="userFilter" type="text" placeholder="Filter by name or email" style="padding:8px; width:60%; max-width:420px; margin:10px auto 20px; display:block;">

  <div style="overflow-x:auto;">
    <table id="usersTable" style="width:100%; border-collapse:collapse; background:#fff;">
      <thead>
        <tr style="background:#eaf5f8;">
          <th style="padding:10px; border-bottom:1px solid #ddd; text-align:left;">First Name</th>
          <th style="padding:10px; border-bottom:1px solid #ddd; text-align:left;">Last Name</th>
          <th style="padding:10px; border-bottom:1px solid #ddd; text-align:left;">Phone</th>
          <th style="padding:10px; border-bottom:1px solid #ddd; text-align:left;">Email</th>
          <th style="padding:10px; border-bottom:1px solid #ddd; text-align:left;">Closed Chakra</th>
        </tr>
      </thead>
      <tbody>
        <% if (typeof users !== 'undefined' && users.length) { %>
          <% users.forEach(function(u){ %>
            <tr>
              <td style="padding:10px; border-bottom:1px solid #f0f0f0;"><%= u.firstname %></td>
              <td style="padding:10px; border-bottom:1px solid #f0f0f0;"><%= u.lastname %></td>
              <td style="padding:10px; border-bottom:1px solid #f0f0f0;"><%= u.phonenumber %></td>
              <td style="padding:10px; border-bottom:1px solid #f0f0f0;"><%= u.email %></td>
              <td style="padding:10px; border-bottom:1px solid #f0f0f0;"><%= u.closedChakra %></td>
            </tr>
          <% }) %>
        <% } else { %>
          <tr><td colspan="5" style="padding:12px;">No users found.</td></tr>
        <% } %>
      </tbody>
    </table>
  </div>
</div>

<script>
  // simple client-side filter
  const filter = document.getElementById('userFilter');
  const table = document.getElementById('usersTable');
  if (filter && table) {
    filter.addEventListener('input', () => {
      const q = filter.value.toLowerCase();
      for (const row of table.tBodies[0].rows) {
        const name = (row.cells[0].textContent + ' ' + row.cells[1].textContent).toLowerCase();
        const email = row.cells[3].textContent.toLowerCase();
        row.style.display = (name.includes(q) || email.includes(q)) ? '' : 'none';
      }
    });
  }
</script>


    <!--edit the resources page -->
    <a href="/adminportal/resourcesmanagement"><button>Manage Resources</button></a>


    <!-- from last sprint, delete 
    <button id="openResourcesBtn">Edit Resources</button>

    
    <div class="subpage" id="subpage">
        <h2>Edit Resources</h2>
        <p>Edit the contents of the Resources page</p>

        <span id="closeBtn" class="close-btn">&times;</span>

        <a href="/gallery"><button>View Uploaded Images</button></a>


    <form id="uploadForm" enctype="multipart/form-data">
        <input type="file" name="photo" accept="image/*" required>
        <button type="submit">Upload Image</button>
      </form>
      
    <div id="uploadStatus"></div>
    </div> -->

    <div class="overview">
        <h2>Add a Testimonial</h2>
        <form action="/admin/add-review" method="POST" class="testimonial-form">
            <label for="name">Name:</label><br>
            <input type="text" id="name" name="name" required><br><br>

            <label for="quote">Quote:</label><br>
            <textarea id="quote" name="quote" rows="3" required></textarea><br><br>

            <label for="location">Location:</label><br>
            <input type="text" id="location" name="location" required><br><br>

            <label for="event">Event:</label><br>
            <input type="text" id="event" name="event" required><br><br>

            <button type="submit">Submit Testimonial</button>
        </form>
    </div>

    <script>
        // Check if redirected with ?submitted=true
        const urlParams = new URLSearchParams(window.location.search);
        if (urlParams.get('submitted') === 'true') {
          alert('✅ Testimonial submitted successfully!');
        }
      </script>
</body>
</html>