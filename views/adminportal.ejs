<!-- adminportal.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>GracefulLiving Dashboard</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Source+Serif+Pro:wght@400;600;700&display=swap" rel="stylesheet">
  
  <!-- css resources page -->
  <link rel="stylesheet" href="/css/adminportal.css">

  <!-- js for resources page -->
  <script src="/js/adminportal.js" defer></script> 

  <!-- chart.js library -->
  <script src="/js/chart.umd.js" defer></script>

  <!-- js for chakra stats -->
  <script src="/js/stats.js" defer></script>

  <style>
    body {
      margin: 0;
      font-family: 'Open Sans', sans-serif; /*using this font general text*/
      min-height: 100vh;
      background: url('/images/pexels-pixabay-358457.jpg') center/cover no-repeat fixed;
    }
    .hero,
    .overview h2,
    .overview-box h3 {
      font-family: 'Playfair Display', serif; /*using this font for the headers*/
    }
    main {
      padding-top: 5rem;
      padding-bottom: 5rem;
    }
    .hero {
      color: white;
      text-align: left;
      padding: 50px 50px 20px;
      font-size: 2rem; /*Welcome*/
    }
    .secondary-nav {
      background-color: #dff3f4cc;
      text-align: center;
      padding: 15px 0;
      font-size: 1.2em;
      margin-bottom: 40px; /* Add space below nav */
    }
    .secondary-nav a {
      margin: 0 50px;
      cursor: pointer;
      text-decoration: none;
      color: inherit;
      font-weight: bold;
      transition: color 0.3s;
    }
    .secondary-nav a:hover {
      color: #1d8888;
    }
    .overview {
      background: rgba(230, 243, 248, 0.8);
      margin: 0 auto 120px;
      padding: 40px;
      width: 80%;
      text-align: center;
      border-radius: 0px;
    }
    .overview-container {
      display: flex;
      justify-content: space-around;
      margin-top: 50px;
      margin-bottom: 50px;
    }
    .overview-box {
      background: white;
      padding: 1px;
      width: 30%;
      border-radius: 0px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.211);
    }
    .overview-box h3 {
      margin-bottom: 10px;
      font-weight: 500;
    }
    .content-box {
      padding: 15px;
      height: 150px;
      border-radius: 5px;
      overflow-y: auto;
    }
    .overview h2 {
      font-weight: 500;
    }
    .btn-add{
      display:inline-block; padding:8px 12px; border-radius:8px;
      background:#0b8; color:#fff; text-decoration:none; font-weight:600;
    }
    .btn-add:hover{ background:#097; }
  </style>
  <!-- conversion prediction section -->
  <link rel="stylesheet" href="/css/conversion-analysis.css">
  <script src="/js/conversion-stats.js" defer></script>
</head>
<body>
  
  <div class="hero">
    <b>Welcome <%= userName %>!</b>
  </div>

  <div class="secondary-nav">
    <!-- <a href="/adminportal">Home</a> -->
    <a href="/content-management">Content Management</a>
    <!--<a href="/adminportal/session">Session Management</a>-->
    <!--<a href="/clientmanagement/add">Add Client</a>-->
    <a href="/clientmanagement">Client Management</a>
  </div>

  <!-- Statistics Dashboard -->

  <section class="overview">

    <div class="chakra-overview">
      <h1>Chakra Imbalance Overview</h1>

      <div class="total-submissions-card">
        Total Submissions:
        <span class="total-number">
          <%= typeof totalSubmissions !== 'undefined' ? totalSubmissions : 0 %>
        </span>
      </div>
    </div>

    <section class="chakra-container-grid">
      <% chakras.forEach(chakra => { %>
        <div class="chakra-card <%= chakra.key.toLowerCase() %>">
          <h3><%= chakra.label %> Chakra</h3>
          <p><span class="chakra-count"><%= chakra.count %></span></p>
        </div>
      <% }) %>
    </section>

  </section>
  
  </main>

  <!-- ML Conversion Analysis Section - Separate, Beautiful UI -->
  <section class="ml-conversion-section">
    <div class="ml-conversion-container">
      <div class="ml-section-header">
        <h2 class="ml-section-title"> Quiz-to-Appointment Conversion Analysis</h2>
        <p class="ml-section-subtitle">AI-Powered insights to help you identify and prioritize high-value leads</p>
        <% if (typeof mlPredictions !== 'undefined' && mlPredictions && mlPredictions.length > 0) { %>
          <div class="ml-status-badge active">ML Predictions Active</div>
        <% } else { %>
          <div class="ml-status-badge">Database Analytics</div>
        <% } %>
      </div>
      
      <!-- Data container - JavaScript will render the content -->
      <div id="conversion-prediction-container"
           data-conversion-stats='<%- typeof conversionStats !== "undefined" && conversionStats ? JSON.stringify(conversionStats) : "null" %>'
           data-ml-predictions='<%- typeof mlPredictions !== "undefined" && mlPredictions ? JSON.stringify(mlPredictions) : "null" %>'
           data-high-probability-leads='<%= typeof highProbabilityLeads !== "undefined" ? highProbabilityLeads : 0 %>'>
        <!-- Loading message -->
        <div class="ml-loading">
          <div class="ml-loading-spinner"></div>
          <p class="ml-loading-text">Loading conversion statistics...</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Chakra Assessment Statistics Section -->
  <section class="stats-dashboard-section">
    <div class="stats-dashboard-header">
      <h2>📊 Chakra Assessment Statistics</h2>

      <div class="stats-controls">
        <div class="month-filter">
          <label for="statsMonthPicker" class="filter-label">Month</label>
          <input type="month" id="statsMonthPicker" class="month-input" />
          <button id="clearMonthBtn" class="btn-secondary" type="button">All time</button>
          <button id="refreshStatsBtn" class="refresh-btn" type="button">
            <span class="refresh-text">Refresh Data</span>
          </button>
        </div>

        <div class="filter-status">
          <span id="filterLabel" class="badge badge-muted">All time</span>
          <span id="submissionCountBadge" class="badge badge-strong">0 submissions</span>
        </div>
      </div>
    </div>
    
    <div id="statsLoadingMessage" class="loading-message">
      Loading fresh statistics...
    </div>

    <div id="statsContent">
      <!-- Charts Grid -->
      <div class="stats-charts-grid">
        <!-- Chakra Radar Chart -->
        <div class="stats-chart-container large">
          <h3>Chakra Balance Overview (Out of 21)</h3>
          <canvas id="chakraRadarChart"></canvas>
        </div>

        <!-- Life Quadrants Bar Chart -->
        <div class="stats-chart-container large">
          <h3>Life Quadrants Analysis (Out of 15)</h3>
          <canvas id="lifeQuadrantsChart"></canvas>
        </div>

        <!-- Archetype Distribution -->
        <div class="stats-chart-container medium">
          <h3>Archetype Distribution</h3>
          <canvas id="archetypeChart"></canvas>
        </div>

        <!-- Focus Chakra Distribution -->
        <div class="stats-chart-container medium">
          <h3>Focus Chakra Distribution</h3>
          <canvas id="focusChakraChart"></canvas>
        </div>

        <!-- Age Distribution -->
        <div class="stats-chart-container medium">
          <h3>Age Distribution</h3>
          <canvas id="ageBracketChart"></canvas>
        </div>

        <!-- Top Challenges -->
        <div class="stats-chart-container medium">
          <h3>Top Challenges</h3>
          <canvas id="challengesChart"></canvas>
        </div>

        <!-- Experience with practitioners -->
        <div class="stats-chart-container medium">
          <h3>Worked with a Holistic Health Practitioner Before?</h3>
          <canvas id="experienceChart"></canvas>
        </div>

        <!-- Familiarity with modalities -->
        <div class="stats-chart-container medium">
          <h3>Familiar With the Following Techniques</h3>
          <canvas id="familiarityChart"></canvas>
        </div>

        <!-- Healthcare Worker -->
        <div class="stats-chart-container medium">
          <h3>Healthcare Workers</h3>
          <canvas id="healthcareChart"></canvas>
        </div>
      </div>

      <!-- Data Tables -->
      <div class="stats-tables">
        <div class="stats-table-wrapper">
          <h3>Chakra Averages (Out of 21)</h3>
          <table class="stats-data-table">
            <thead>
              <tr>
                <th>Chakra</th>
                <th>Average Score</th>
                <th>Mode Score</th>
              </tr>
            </thead>
            <tbody id="chakraTableBody">
              <tr><td colspan="3">Loading...</td></tr>
            </tbody>
          </table>
        </div>

        <div class="stats-table-wrapper">
          <h3>Life Quadrant Averages (Out of 15)</h3>
          <table class="stats-data-table">
            <thead>
              <tr>
                <th>Life Quadrant</th>
                <th>Average Score</th>
                <th>Mode Score</th>
              </tr>
            </thead>
            <tbody id="quadrantTableBody">
              <tr><td colspan="3">Loading...</td></tr>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </section>

 
<!-- rest of homepage content -->
  <!-- CHAKRA PREDICTION -->
  <div style="margin: 1rem 0;">
    <button id="showTrendBtn" class="btn btn-info">📊 View Chakra Trends</button>
  </div>

  <!-- Hidden iframe (starts invisible) -->
  <iframe
    id="trendFrame"
    src="/adminportal/predict-chakra"
    style="display:block;width:100%;height:400px;border:1px solid #ccc;border-radius:8px;">
  </iframe>

 
 
        



  <!--edit the resources page - Oanh moved it into content-management.ejs-->
  <!-- <a href="/adminportal/resourcesmanagement"><button>Manage Resources</button></a> -->

  <!-- from last sprint, delete 
  <button id="openResourcesBtn">Edit Resources</button>

  <div class="subpage" id="subpage">
      <h2>Edit Resources</h2>
      <p>Edit the contents of the Resources page</p>

      <span id="closeBtn" class="close-btn">&times;</span>

      <a href="/gallery"><button>View Uploaded Images</button></a>

      <form id="uploadForm" enctype="multipart/form-data">
          <input type="file" name="photo" accept="image/*" required>
          <button type="submit">Upload Image</button>
      </form>
      
      <div id="uploadStatus"></div>
  </div> -->

  <!--sprint 06 - Oanh moved it into content-management.ejs-->
  <!-- <div class="overview">
      <h2>Add a Testimonial</h2>
      <form action="/admin/add-review" method="POST" class="testimonial-form">
          <label for="name">Name:</label><br>
          <input type="text" id="name" name="name" required><br><br>

          <label for="quote">Quote:</label><br>
          <textarea id="quote" name="quote" rows="3" required></textarea><br><br>

          <label for="location">Location:</label><br>
          <input type="text" id="location" name="location" required><br><br>

          <label for="event">Event:</label><br>
          <input type="text" id="event" name="event" required><br><br>

          <button type="submit">Submit Testimonial</button>
      </form>
  </div> -->

  <!-- <script>
      // Check if redirected with ?submitted=true
      const urlParams = new URLSearchParams(window.location.search);
      if (urlParams.get('submitted') === 'true') {
        alert('✅ Testimonial submitted successfully!');
      }
    </script> -->
</body>
</html>
